start separating each function into separate html files:
    tasks,
    focus mode,
    calendar & summary,
    settings & integrations

focus mode:

should be greeted with 

timer panel:
    timer phase,
    timer display,
    timer session counter,
    buttons,
        start and pause should be the same button, it alternates depending on the state of the timer

on the top left, there should be settings ("cog") button, that when pressed popsover the timer and shows the individual settings 